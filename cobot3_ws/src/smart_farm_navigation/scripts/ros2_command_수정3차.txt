=================================================================================
🌱 Smart Farm MiR100 내비게이션 & Isaac Sim 실행 가이드 [수정3차]
=================================================================================

1. [18:30 오류 원인 및 조치]
---------------------------------------------------------------------------------
1) USD 파일 미오픈 현상:
   - 원인: Omniverse Kit 실행 시 파일 경로 앞에 설정 플래그가 누락되어 기본 빈 창으로 뜸.
   - 조치: Kit 표준 파라미터인 `--/app/file/open="$USD_PATH"` 를 적용하여 씬 자동 로드 완료.
2) ROS_DOMAIN_ID 불일치:
   - 고사양 PC의 DOMAIN_ID(101)와 스크립트 간 불일치 문제 해결 (${ROS_DOMAIN_ID:-101} 자동 보존).
3) Action Graph 1회 생성 (스크립트 에디터 대신 내장 메뉴 사용):
   - 상단 메뉴: Isaac Utils -> Common Omnigraphs -> ROS2 Differential Base
   - Target Prim: /World/mir100 지정 후 OK -> Ctrl + S 저장 (영구 보존).


=================================================================================
2. [고사양 PC 실행 - 코드 한 줄]
=================================================================================

터미널 1:
    cd ~/ROKEY_P3_A1 && git pull origin feature/lwh
    bash ~/ROKEY_P3_A1/cobot3_ws/src/smart_farm_navigation/scripts/run_all.sh

* 실행 흐름:
  1) Isaac Sim이 실행되며 Collected_260916_AMR_test.usd 스테이지를 자동으로 엽니다.
  2) 스테이지가 로드되면 Play(▶)를 누릅니다.
  3) /clock 토픽 감지 즉시 Nav2 & RViz2가 켜지며 맵과 로봇이 정상 표시됩니다.


=================================================================================
3. [로봇 자율 주행 실행 - 터미널 2]
=================================================================================

터미널 2:
    export ROS_DISTRO=jazzy
    export ROS_DOMAIN_ID=101
    export RMW_IMPLEMENTATION=rmw_fastrtps_cpp
    export FASTRTPS_DEFAULT_PROFILES_FILE=$HOME/.ros/fastdds_whitelist.xml
    ros_set
    source ~/ROKEY_P3_A1/cobot3_ws/install/setup.bash

    ros2 run nav_to_goal nav_to_pose
=================================================================================
