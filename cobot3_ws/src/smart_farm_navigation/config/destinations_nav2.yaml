# navigation_node (mode:=nav2): destination 이름 -> config/stations.yaml 의 station. Nav2 가 먼저 떠 있어야 함 (nav2.launch.py).
destinations:
  FEEDER_DOCK:     {station: FEEDER_DOCK}
  INSPECTION_DOCK: {station: INSPECTION_DOCK}
  RACK_DOCK:       {station: RACK_DOCK}
  CORRIDOR_EXIT:   {station: CORRIDOR_EXIT}
