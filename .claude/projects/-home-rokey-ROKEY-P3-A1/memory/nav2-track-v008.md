---
name: nav2-track-v008
description: Nav2 track state on feature/navigation2 (2026-09-22) — v011 scene + generated v011 map, FEEDER_DOCK goal, field-run fixes, open issues
metadata:
  type: project
---

Nav2 track (feature/navigation2, guidance2_<n>차 starting at 12차). Rules live in docs/ADR/ADR_navigation2.md (scene Collected_smartfarm_v008.usd, map isaacpjt/smart_farm/maps/Collected_smartfarm_v005.yaml; ADR_basic wins on conflict; do not edit ADR_nav2 without approval).

- Split: 고피 runs only Isaac (`isaac_python scripts/launch_scene.py`, which adds a runtime /clock graph because v002+ scenes have no ROS_Clock); 내피 runs Nav2/RViz2 (`nav2.launch.py`, use_sim_time). 내피 wired IP 10.10.0.3 is in the FastDDS whitelist; bashrc domain is 103 so every terminal exports 101. ros_set vs isaac_ros question is on hold: follow the tutor, do not prescribe.
- v008 facts: one carter (LiftRig/Asset/nova_carter_ROS) at (−0.421, 1.006) yaw 90, no ROS namespace; conveyor shifted −1.63 m in y (north edge −6.18) and segments renamed (old Seg_6 spot is Seg_4); TurnTable added. Lift posts surround the XT-32 and RPLidars (self-returns ≤0.53 m) → scan_sanitizer / range_min 0.4.
- Rig must not turn in place inside the rack corridor (rear sweep 0.66 m; USD rack/pallet edge x −0.86 while the map shows −1.10). go_to_station backs out with Nav2 BackUp first (stations.yaml reverse_out_zones) and RACK_DOCK goes via CORRIDOR_EXIT.
- Controller is RPP (DWB dithered at the goal). Verified only with a synthetic robot on 내피 (scratchpad fake_isaac.py, wiped on reboot); synthetic AMCL error 0.2–0.35 m at the dock is unexplained. Real Isaac run pending.

**Why:** None of this is derivable from code alone; the user hands over values by prompt because Isaac time is scarce.

**How to apply:** Keep new work in guidance2_<n>차; ask for the real-run logs (results/link_*, nav2_*, goto_*) before tuning further. See [[project-milestones]], [[carter-visual-front]], [[gopi-naepi-split]].

- 2026-09-22 field feedback (errored/error_260921_2100.md) applied: scene now Collected_smartfarm_v011 (adds inspection cell walls BackWall_03..07 and room walls); map regenerated by scripts/make_map_from_usd.py as maps/Collected_smartfarm_v011 (ADR_nav2 1.2 still says v005 — user to decide). Real run showed RPP rotate-to-heading crawling (fixed with max_angular_accel 20 + PoseProgressChecker), goal dithering at 0.16 m (xy tol 0.25), 180 s wall timeout too short at rtf 0.4 (600 s), Isaac restart requires Nav2 restart. New goal FEEDER_DOCK = north end of Conveyor/TurnTable, carter front (+x) facing it at 0.55 m; arm reach unverified (flip yaw to 90 if the arm must face it). 2D lidar scan never arrived on 고피; cloud mode (1.3 MB/s) works over the wire.
- User's guidance feedback: explain what each 방법 is for; 방법 B is optional, do not make them run both.
- 2026-09-22 (later): development merged into feature/navigation2; rule from the user: on merge conflicts the pulled (development) files win, re-add my additions on top (navigation_node is now the team's TaskCommand version + my `station` branch). Team runtime `isaacpjt/smart_farm/runtime/standalone_app.py` owns Isaac for integration (rclpy inside Isaac, /sim_task/command PICK_HARVEST → carry pose, releases wheels, safe_to_navigate). v011 refreshed: has ROS_Clock graph, cameras removed. Feeder docking is REAR-IN (lift self-collision if the front faces it): Nav2 to FEEDER_APPROACH (−2.19,−1.55,90°) then BackUp 1.17 m to FEEDER_DOCK (−2.19,−2.72,90°); rear must stay ≥0.27 m from the TurnTable (BackUp aborts inside the 0.25 m inscribed band). 3D lidar 10 Hz via fullScan at runtime; cloud_self_filter box removes rig/pallet points. Tutor-mandated 3 tests (basic / carry pose via arm_poses.yaml / real pick via team app) = guidance2_14차; 13차 moved to past. Test hygiene: stale go_to_station from a killed run caused BackUp preemption ping-pong; publish TaskCommand with -t 3 because the bag recorder also subscribes.
- Tutor condition (2026-09-22): all 3 tests start at the rig's scene initial pose and must be driven by Nav2 ONLY (no scripted BackUp). Done via Smac Hybrid-A* (REEDS_SHEPP, allow_reverse_expansion, reverse_penalty 1.0) + RPP allow_reversing; go_to_station pure_nav2 default true (scripted BackUp kept behind pure_nav2:=false). Global inflation must stay 0.45 (0.70 made corridor return 99 s).
- 2026-09-22 14차 field failure root causes (from bag nav2_20260922_1335): launch_scene.py did not hold M0609 joint drives → arm sagged, self points grew 114→313/scan and left the filter box → lethal cells on the rig → RPP "collision ahead" + AMCL 0.55 m off (user re-clicked 2D Pose Estimate 3×). Curved RS path end left the dock 33° off (RPP cannot rotate in place with allow_reversing). Fixes: hold drives (stiffness 1e8 like team), box x −1.1..0.6 y ±0.6, FEEDER_APPROACH via + straight 1.05 m reverse to FEEDER_DOCK y −2.60, yaw tol 0.5, speed 0.6 m/s accel 0.8. Team /sim_task/command is std_msgs/String JSON (not TaskCommand); Isaac Stop crashes the team app. --pose now uses USD DriveAPI targets (articulation API crashed without a World).
- 2026-09-22 evening: precise docking = feeder_dock.py (RANSAC line on /scan for the TurnTable north face, /cmd_vel rear-in, auto-arm near FEEDER_APPROACH, standoff 1.0 m). Self-filter box rear must be > −0.85 or the face at 1.0 m vanishes. 2D lidar never published on 고피 (4 runs). launch_scene _Tee needs fileno() for SimulationApp. guidance2_16차.
- 2026-09-22 night: field tests moved to 고피2 with ROS_DOMAIN_ID **102** (use 102 in all guidance/env lines from now). Full pipeline (team standalone_app + PICK_HARVEST + RViz click FEEDER_APPROACH + auto feeder_dock) succeeded 2 of 3; one run turned and drove forward mid-dock (unresolved, hypothesis: face detection jumped to another line; add continuity check). Docs: docs/architecture.md (Nav2 판) + troubleshooting.md §5.
- 2026-09-23 integration branch: team lead set `nav2.launch.py` `record` default **false** (pass `record:=true` for runs that need analysis) and `navigation_node` ExecutorStatus state `EXECUTING` → **BUSY**. Both mirrored locally (commit e8120bf).
- Rule updates from the user: (a) nothing is excluded from commits/push any more — push the whole tree each turn, but always report what went in via the guidance or the answer; still never `git pull`. (b) After integration the user will fold Nav2 into `standalone_app.py`, so RViz2 Nav2 Goal clicking stops being the standard procedure; current phase is module stability testing on the already-integrated code.
- 2026-09-23: work moved to branch **feature/Inspection-Place-nav2** (integration branch; contains all of feature/navigation2 plus the team's PLACE_INSPECT flow and smart_farm_vision). ADR_navigation2 now applies on both branches, and more generally to anything Nav2-related regardless of branch name — read docs/ADR/ADR_navigation2.md together with ADR_basic.md whenever Nav2 is involved.
- 2026-09-23 integration done (local commit, push blocked by remote ahead): navigation_node now drives NavigateToPose itself (no subprocess; BasicNavigator cannot be used inside a timer callback — re-entrant executor), triggers feeder_dock with the command_id as run_id and only accepts a matching result (the result topic is latched, so an old result would otherwise read as instant success). standoff 0.75 paired with self_box_x [-0.60, 0.60]; arm reach at that distance is exactly 0.90 m (M0609 limit) so fall back to 0.70 / -0.55 if IK fails. Timeouts raised (NAVIGATION step 400 s, node 600 s) because 0.3 m/s × RTF 0.3 exceeds the old 120 s. Duplicate command_id while running is now ignored (BUSY was being cached and masking the real result). 1차 시연 configs/launches moved to past/. Field log to collect next: the "[도킹] 카터 본체 world …" line from standalone_app, to set the arm pose tolerance.
- 2026-09-23 19차 field run: drive + dock SUCCEEDED (face_dist 0.758 m, yaw_err 0.31 deg, lat -0.086 m) — the 0.75 standoff works. Isaac then died the instant PLACE_INSPECT was sent: my report_dock_pose() called robot_motion.prim_world_pose (module not imported there, and that function does not exist anywhere) plus numpy which standalone_app never imports; the exception reached __main__ and closed the app. Traceback was lost because stdout through `tee` is block-buffered — always run Isaac with PYTHONUNBUFFERED=1 + stdbuf -oL -eL. Also: BaseWatcher is owned by PalletTransferController; never update it from the main loop (use a separate instance). Run `python3 -m pyflakes` on any Isaac-side file before shipping — those files cannot be executed here.
- `isaac_python` is a bash alias in 고피's ~/.bashrc. Aliases expand only as the FIRST word of a command, so `stdbuf -oL isaac_python ...` fails with ENOENT. Never put an external command in front of it; use environment variables (PYTHONUNBUFFERED=1) or `isaac_python -u` for unbuffered output instead.
